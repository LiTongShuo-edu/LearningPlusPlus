# 手写旧作编译记录

## 来源与处理方式

`代码案例/` 中无后缀的 34 份源码来自本人于 2019 至 2020 年在旧电脑上编写、后经个人 OneDrive 保留的早期手写练习。发布整理时去除了 Visual Studio 工程重复目录层级，并将需要转换的文本保存为 UTF-8。其余随旧电脑报废而遗失的练习代码，以 `＊` 标记其在 Codex 协助下重新复现的公开版本。

2026-05-26 进行逐文件正文比对时，34/34 份公开源码与原始源码解码后的文字内容一致。后续为使旧作可独立编译，`代码案例/案例-猜数字/源.cpp` 在 Codex 协助下补充一个缺失的循环闭合大括号；该改动不改变游戏逻辑。

## 编译尝试结果

复核日期：2026-05-27。验证环境：Visual Studio 2022 / MSVC，使用 C++17 单独编译每份 `.cpp` 文件。

| 项目 | 结果 |
| --- | --- |
| 源码总数 | 34 |
| 编译通过 | 34 |
| 编译失败 | 0 |

修复项：

| 源码 | 原问题 | 修复处理 |
| --- | --- | --- |
| `代码案例/案例-猜数字/源.cpp` | `while` 循环缺少闭合大括号，导致编译失败 | 在 Codex 协助下补充闭合大括号，保留原有流程与输出 |

该记录用于区分仍保留的历史练习、丢失后复现的 `＊` 练习与岗位扩展的 `^` 示例。公开展示的 `＊` 与 `^` 代码由顶层 CMake 单独验证。
