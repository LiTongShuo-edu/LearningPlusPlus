# 手写旧作编译记录

## 来源与处理方式

`代码案例/` 中无后缀的 34 份源码来自本人于 2019 至 2020 年在旧电脑上编写、后经个人 OneDrive 保留的早期手写练习。发布整理时去除了 Visual Studio 工程重复目录层级，并将需要转换的文本保存为 UTF-8；未修复源程序逻辑、输出文本或编译问题。其余随旧电脑报废而遗失的练习代码，以 `＊` 标记其在 Codex 协助下重新复现的公开版本。

2026-05-26 进行逐文件正文比对，34/34 份公开源码与原始源码解码后的文字内容一致。

## 编译尝试结果

验证环境：Visual Studio 2022 / MSVC，使用 C++17 单独编译每份 `.cpp` 文件。

| 项目 | 结果 |
| --- | --- |
| 源码总数 | 34 |
| 编译通过 | 33 |
| 编译失败 | 1 |

失败项：

| 源码 | 客观结果 | 处理决定 |
| --- | --- | --- |
| `代码案例/案例-猜数字/源.cpp` | 编译器报告 `{` 未找到匹配令牌 | 保持早期源码原状，不为发布修改 |

该记录用于区分仍保留的历史练习、丢失后复现的 `＊` 练习与岗位扩展的 `^` 示例。公开展示的 `＊` 与 `^` 代码由顶层 CMake 单独验证。
